class Transaction {
  int id;
  String transactionDate;
  String name;
  String amount;
  String company;
  int rating;

  Transaction(this.id, this.transactionDate, this.name, this.amount,
      this.company, this.rating);
}
