class Message {
  int messageId;
  String sender;
  String recepient;
  String message;
  bool isReceiver;

  Message(this.messageId, this.sender, this.recepient, this.message,
      this.isReceiver);
}
